import { TargetLanguageLevel } from "../student/TargetLanguageLevel";

export const simplifyContentSystemMessage = (level: TargetLanguageLevel) => `Simplify content you are provided with for a ${level} language learner.`;
